[
  {
    "name": "code-review",
    "description": "これはテストの内容です。",
    "disable-model-invocation": "true",
    "context": "fork",
    "content": "# Code Review\n\n## レビュー対象の決定\n\n$ARGUMENTSが指定されている場合はそのファイル・ディレクトリを対象にする。\n- ファイルパスが渡された場合 → Read ツールでそのファイルを読む\n- ディレクトリパスが渡された場合 → Glob でファイル一覧を取得してから Read で各ファイルを読む\n\n$ARGUMENTSが指定されていない場合は、現在のワーキングディレクトリのコードファイルを対象にする：\n1. !`find . -type f \\( -name \"*.py\" -o -name \"*.js\" -o -name \"*.ts\" -o -name \"*.jsx\" -o -name \"*.tsx\" -o -name \"*.vue\" -o -name \"*.rb\" -o -name \"*.lua\" -o -name \"*.bas\" -o -name \"*.cls\" -o -name \"*.frm\" \\) -not -path \"*/node_modules/*\" -not -path \"*/.git/*\" -not -path \"*/__pycache__/*\" -not -path \"*/.venv/*\" 2>/dev/null | head -30`\n2. 上記で見つかったファイルを Read ツールで読む\n3. ファイルが多い場合は、ユーザーに「どのファイル・ディレクトリを対象にしますか？」と確認してから進める（目安: 5ファイル超は確認する）\n\n---\n\n## レビュー手順\n\n### Step 1: 言語・技術スタックの判定\n\n対象ファイルの拡張子から使用言語を判定する：\n- `.py` → Python\n- `.js` / `.ts` / `.jsx` / `.tsx` → JavaScript/TypeScript\n- `.vue` → Vue.js\n- `.bas` / `.cls` / `.frm` / `.vba` → VBA\n- `.lua` → Lua\n- `.rb` → Ruby\n- `.html` / `.hbs` → HTML/Handlebars\n- その他はファイル拡張子から判断\n\n---\n\n### Step 2: 🚨 環境設定チェック【最優先・最初に確認】\n\n**Monitoring モジュール等のURL設定を本番リリース前に必ず確認すること。**\n違反があれば他のチェックより先に最優先で警告を出す。\n\n> ⚠️ 本番URLがコメントアウトされている、または検証URLが有効になっている場合は **🚨 クリティカル** として報告する。\n\n---\n\n### Step 3: 共通チェック（全言語）\n\n- [ ] デバッグコードが残っていないか（`print`, `console.log`, `Debug.Print`, `pp` 等）\n- [ ] コメントアウトされたままの不要なコードが残っていないか\n- [ ] ハードコードされた認証情報・APIキー・パスワードがないか\n- [ ] 不要なファイル（`.DS_Store`, `__pycache__`, `*.pyc`, `node_modules` 等）が含まれていないか\n- [ ] 変数名・関数名が意味を適切に表しているか（`i`, `r`, `x`, `tmp`, `hoge` 等の1文字・無意味な名前は禁止）\n\n---\n\n### Step 4: 言語固有チェック\n\n#### Python の場合（PEP8 / Google Python Style Guide）\n\n**スタイル**\n- [ ] インデントは4スペースか\n- [ ] 1行の長さが適切か（PEP8: 79文字、black: 88文字が目安）\n- [ ] インポートは stdlib → サードパーティ → ローカルの順に整理されているか（isort）\n- [ ] `import *` を使っていないか\n- [ ] 不要なインポートがないか\n\n**命名規則**\n- [ ] 変数・関数名は `snake_case` か\n- [ ] クラス名は `PascalCase` か\n- [ ] 定数は `UPPER_SNAKE_CASE` か\n\n**コード品質**\n- [ ] 例外処理が適切か（裸の `except:` や `except Exception:` になっていないか）\n- [ ] `f-string` や適切な文字列フォーマットを使っているか\n- [ ] mutable なデフォルト引数（`def f(x=[]):`）を使っていないか\n- [ ] `is None` / `is not None` を使っているか（`== None` は不可）\n\n---\n\n#### JavaScript / TypeScript の場合（ESLint / Prettier）\n\n**スタイル**\n- [ ] インデントは2スペースか\n- [ ] 文末セミコロンのスタイルが統一されているか\n\n**命名規則**\n- [ ] 変数・関数名は `camelCase` か\n- [ ] クラス名は `PascalCase` か\n- [ ] 定数は `UPPER_SNAKE_CASE` か\n\n**コード品質**\n- [ ] `var` を使っていないか（`const` / `let` を使うべき）\n- [ ] 非同期処理が適切か（`async/await` を優先）\n- [ ] `==` ではなく `===` を使っているか\n- [ ] エラーハンドリングが適切か（Promise の `.catch()` や `try/catch`）\n\n**TypeScript 固有**\n- [ ] `any` 型を使っていないか（`unknown` を検討）\n- [ ] non-null アサーション（`!`）が過剰でないか\n\n**セキュリティ**\n- [ ] XSS リスクがないか（`innerHTML`, `dangerouslySetInnerHTML` の不適切な使用）\n- [ ] `eval()` を使っていないか\n\n---\n\n#### VBA の場合（Microsoft Office VBA コーディング規約）\n\n**スタイル**\n- [ ] インデントは4スペース（またはタブ）で統一されているか\n- [ ] 長い行は `_` で適切に折り返されているか\n\n**命名規則**\n- [ ] 変数名はハンガリアン記法またはキャメルケースで統一されているか\n- [ ] 定数名は `UPPER_SNAKE_CASE` か\n- [ ] プロシージャ名・関数名は動詞で始まる `PascalCase` か\n\n**コード品質**\n- [ ] `Option Explicit` が全モジュールに宣言されているか\n- [ ] 変数の型が明示的に宣言されているか（`Dim x As Long` 等）\n- [ ] エラーハンドリング（`On Error GoTo`）が適切か（`On Error Resume Next` の多用は不可）\n- [ ] マジックナンバーが使われていないか（`Const` で定数化されているか）\n- [ ] ワークシートへの参照が `ActiveSheet` に依存していないか\n\n**エンコーディング**\n- [ ] VBA ファイル（`.bas` / `.cls` / `.frm`）内の日本語はSHIFT-JIS（CP932）で出力されているか（UTF-8で書き込むと Windows VBAエディタで文字化けする）\n\n> ⚠️ **VBAファイルを修正・新規作成する場合は必ずSHIFT-JIS（CP932）で書き込むこと。**\n> Edit/Write ツールは UTF-8 で書き込むため、VBAファイルには使用禁止。\n> 代わりに以下の Bash コマンドで書き込む：\n> ```bash\n> python3 -c \"\n> content = '''（VBAコード）'''\n> with open('ファイルパス', 'w', encoding='cp932') as f:\n>     f.write(content)\n> \"\n> ```\n\n---\n\n#### Lua の場合\n\n**命名規則**\n- [ ] 変数・関数名は `snake_case` か\n- [ ] 定数は `UPPER_SNAKE_CASE` か\n\n**コード品質**\n- [ ] グローバル変数の使用が最小限か（`local` が適切に使われているか）\n- [ ] `pcall` / `xpcall` によるエラーハンドリングが適切か\n- [ ] `nil` チェックが適切に行われているか\n\n---\n\n#### Ruby の場合\n\n**命名規則**\n- [ ] 変数・メソッド名は `snake_case` か\n- [ ] クラス・モジュール名は `PascalCase` か\n\n**コード品質**\n- [ ] `rescue` で例外クラスが明示されているか（裸の `rescue` は不可）\n- [ ] テストが追加・更新されているか\n\n---\n\n### Step 5: Find Bug（バグ検出）\n\n以下の観点でコードを精査し、潜在的なバグを特定する：\n\n**境界値・数値**\n- [ ] ゼロ除算のリスクがないか（除算前に分母のゼロチェックがあるか）\n- [ ] 配列・コレクションの境界値アクセスが安全か（off-by-one エラー）\n- [ ] 数値オーバーフロー・アンダーフローのリスクがないか\n- [ ] 浮動小数点の比較に `==` を使っていないか\n\n**null / 未初期化**\n- [ ] null / Nothing / None 参照のリスクがないか\n- [ ] 変数が初期化される前に参照されていないか\n- [ ] 関数の戻り値が null / Nothing になりうる場合に呼び出し元でチェックしているか\n\n**条件分岐・制御フロー**\n- [ ] 条件分岐に漏れがないか（`else` / `default` の未処理ケース）\n- [ ] 複合条件の論理演算子（`And` / `Or` / `&&` / `||`）が意図通りか\n- [ ] `switch` / `Select Case` に `default` / `Case Else` があるか\n\n**ループ**\n- [ ] 無限ループになるリスクがないか（終了条件が必ず満たされるか）\n- [ ] ループカウンタの更新が適切か\n- [ ] コレクションをループ中に変更していないか\n\n**リソース管理**\n- [ ] ファイル・DB接続・オブジェクトが確実にクローズ・解放されているか\n- [ ] エラー発生時にもリソースが解放されるか（`Finally` / `On Error` での後処理）\n\n**非同期・並行処理**\n- [ ] 競合状態（Race Condition）のリスクがないか\n- [ ] 非同期処理の結果を待たずに次の処理に進んでいないか\n\n**その他**\n- [ ] コピー＆ペーストミス（似たブロックで変数名だけ変え忘れ等）がないか\n- [ ] 再帰処理に終了条件があるか（スタックオーバーフローのリスク）\n\n---\n\n### Step 6: スペルチェック\n\nコード内の識別子・コメント・文字列リテラルのスペルミスを検出する：\n\n**識別子（変数名・関数名・定数名・クラス名）**\n- [ ] 明らかなタイポがないか（例: `recieve` → `receive`、`caluculate` → `calculate`）\n- [ ] 省略形が一貫しているか（例: `msg` と `message` が混在していないか）\n- [ ] 単数・複数形が意味に沿っているか（例: コレクションは複数形）\n\n**コメント・文字列リテラル**\n- [ ] コメント内の英単語にスペルミスがないか\n- [ ] 日本語コメントに誤字・脱字がないか\n- [ ] ユーザー向けメッセージ・ログ文字列に誤字がないか\n\n---\n\n### Step 7: チーム固有ルールチェック\n\n#### ルール1: マジックバリューの排除（重要度で区別）\n\n**【重要度：高】ロジックに影響するもの → 厳しく指摘**\n- [ ] セルの列番号・行番号・セル位置が直書きされていないか\n- [ ] シート名（例: `\"Sheet1\"`, `\"Data\"`）が直書きされていないか\n- [ ] ファイルパス・ループ制御値が直書きされていないか\n\n**【重要度：低】表示のみのもの → 指摘優先度「低」**\n- [ ] メッセージ文言（例: `\"処理完了\"`, `\"エラーです\"`）の直書き（指摘はするが必須修正ではない）\n\n#### ルール2: DRY原則（重複コードの排除）\n- [ ] 似たような処理が2回以上登場している場合、関数へ切り出されているか\n- [ ] コピー＆ペーストで量産されたコードブロックがないか\n\n#### ルール3: モジュールの機能別分割（単一責任の原則）\n- [ ] 1つのモジュール・プロシージャに「UI/View操作」「ビジネスロジック」「インフラ/アクセス」が混在していないか\n- [ ] 異なる種類の責務が混在している場合、分離・委譲の構成を提案すること\n\n---\n\n### Step 8: ロジック・設計レビュー\n\n- バグやロジックの問題がないか\n- 可読性・保守性の観点で改善できる点はないか\n- パフォーマンス上の懸念点はないか（N+1クエリ、不要なループ等）\n- セキュリティ上のリスクはないか（入力バリデーション、SQLインジェクション等）\n- 既存コードとの一貫性が保たれているか\n\n---\n\n## 出力フォーマット\n\n```\n## コードレビュー結果\n\n**対象**: （レビューしたファイル名一覧）\n**対象言語**: 検出した言語\n\n---\n\n### 🚨 クリティカルな指摘（環境設定違反）\n| 行番号 | 重要度 | 指摘内容 |\n| :--- | :--- | :--- |\n| ... | **最高** | ... |\n（該当なければ「なし」）\n\n---\n\n### 共通チェック\n| 項目 | 結果 | コメント |\n|------|------|----------|\n| ... | OK / NG / 該当なし | ... |\n\n### 言語固有チェック（言語名）\n| 項目 | 結果 | コメント |\n|------|------|----------|\n| ... | OK / NG / 該当なし | ... |\n\n### Find Bug（バグ検出）\n（検出したバグ・潜在的リスクをファイル名・行番号付きで記載。問題なければ「特記事項なし」）\n\n### スペルチェック\n（スペルミス・誤字脱字をファイル名・行番号・修正案付きで記載。問題なければ「特記事項なし」）\n\n### ❌ チーム規約違反\n| 行番号 | 重要度 | 違反ルール | 指摘内容 |\n| :--- | :--- | :--- | :--- |\n| ... | **高** / **低** | ... | ... |\n（該当なければ「なし」）\n\n### ✅ 修正案（コードスニペット）\n（上記指摘を反映した修正コード。指摘がなければ省略）\n> ⚠️ VBAファイルを実際に修正する場合は Edit/Write ツールを使わず、必ず `python3 -c \"... encoding='cp932' ...\"` で SHIFT-JIS として書き込むこと。\n\n### ロジック・設計レビュー\n（気になった点を具体的に記載。問題なければ「特記事項なし」）\n\n### 改善提案（任意）\n- （必須ではないが改善すると良い点）\n\n---\n\n### 📝 総評\n- **総合評価**: 問題なし / 軽微な修正推奨 / 要修正\n- **品質スコア**: XX / 100\n- **コメント**: （全体を通じた所感・優先して対応すべき点）\n```",
    "path": "./code-review/SKILL.md"
  },
  {
    "name": "code-copy",
    "description": "これはテストの内容である。",
    "disable-model-invocation": "true",
    "context": "fork",
    "content": "# Code Review\n\n## レビュー対象の決定\n\n$ARGUMENTSが指定されている場合はそのファイル・ディレクトリを対象にする。\n- ファイルパスが渡された場合 → Read ツールでそのファイルを読む\n- ディレクトリパスが渡された場合 → Glob でファイル一覧を取得してから Read で各ファイルを読む\n\n$ARGUMENTSが指定されていない場合は、現在のワーキングディレクトリのコードファイルを対象にする：\n1. !`find . -type f \\( -name \"*.py\" -o -name \"*.js\" -o -name \"*.ts\" -o -name \"*.jsx\" -o -name \"*.tsx\" -o -name \"*.vue\" -o -name \"*.rb\" -o -name \"*.lua\" -o -name \"*.bas\" -o -name \"*.cls\" -o -name \"*.frm\" \\) -not -path \"*/node_modules/*\" -not -path \"*/.git/*\" -not -path \"*/__pycache__/*\" -not -path \"*/.venv/*\" 2>/dev/null | head -30`\n2. 上記で見つかったファイルを Read ツールで読む\n3. ファイルが多い場合は、ユーザーに「どのファイル・ディレクトリを対象にしますか？」と確認してから進める（目安: 5ファイル超は確認する）\n\n---\n\n## レビュー手順\n\n### Step 1: 言語・技術スタックの判定\n\n対象ファイルの拡張子から使用言語を判定する：\n- `.py` → Python\n- `.js` / `.ts` / `.jsx` / `.tsx` → JavaScript/TypeScript\n- `.vue` → Vue.js\n- `.bas` / `.cls` / `.frm` / `.vba` → VBA\n- `.lua` → Lua\n- `.rb` → Ruby\n- `.html` / `.hbs` → HTML/Handlebars\n- その他はファイル拡張子から判断\n\n---\n\n### Step 2: 🚨 環境設定チェック【最優先・最初に確認】\n\n**Monitoring モジュール等のURL設定を本番リリース前に必ず確認すること。**\n違反があれば他のチェックより先に最優先で警告を出す。\n\n> ⚠️ 本番URLがコメントアウトされている、または検証URLが有効になっている場合は **🚨 クリティカル** として報告する。\n\n---\n\n### Step 3: 共通チェック（全言語）\n\n- [ ] デバッグコードが残っていないか（`print`, `console.log`, `Debug.Print`, `pp` 等）\n- [ ] コメントアウトされたままの不要なコードが残っていないか\n- [ ] ハードコードされた認証情報・APIキー・パスワードがないか\n- [ ] 不要なファイル（`.DS_Store`, `__pycache__`, `*.pyc`, `node_modules` 等）が含まれていないか\n- [ ] 変数名・関数名が意味を適切に表しているか（`i`, `r`, `x`, `tmp`, `hoge` 等の1文字・無意味な名前は禁止）\n\n---\n\n### Step 4: 言語固有チェック\n\n#### Python の場合（PEP8 / Google Python Style Guide）\n\n**スタイル**\n- [ ] インデントは4スペースか\n- [ ] 1行の長さが適切か（PEP8: 79文字、black: 88文字が目安）\n- [ ] インポートは stdlib → サードパーティ → ローカルの順に整理されているか（isort）\n- [ ] `import *` を使っていないか\n- [ ] 不要なインポートがないか\n\n**命名規則**\n- [ ] 変数・関数名は `snake_case` か\n- [ ] クラス名は `PascalCase` か\n- [ ] 定数は `UPPER_SNAKE_CASE` か\n\n**コード品質**\n- [ ] 例外処理が適切か（裸の `except:` や `except Exception:` になっていないか）\n- [ ] `f-string` や適切な文字列フォーマットを使っているか\n- [ ] mutable なデフォルト引数（`def f(x=[]):`）を使っていないか\n- [ ] `is None` / `is not None` を使っているか（`== None` は不可）\n\n---\n\n#### JavaScript / TypeScript の場合（ESLint / Prettier）\n\n**スタイル**\n- [ ] インデントは2スペースか\n- [ ] 文末セミコロンのスタイルが統一されているか\n\n**命名規則**\n- [ ] 変数・関数名は `camelCase` か\n- [ ] クラス名は `PascalCase` か\n- [ ] 定数は `UPPER_SNAKE_CASE` か\n\n**コード品質**\n- [ ] `var` を使っていないか（`const` / `let` を使うべき）\n- [ ] 非同期処理が適切か（`async/await` を優先）\n- [ ] `==` ではなく `===` を使っているか\n- [ ] エラーハンドリングが適切か（Promise の `.catch()` や `try/catch`）\n\n**TypeScript 固有**\n- [ ] `any` 型を使っていないか（`unknown` を検討）\n- [ ] non-null アサーション（`!`）が過剰でないか\n\n**セキュリティ**\n- [ ] XSS リスクがないか（`innerHTML`, `dangerouslySetInnerHTML` の不適切な使用）\n- [ ] `eval()` を使っていないか\n\n---\n\n#### VBA の場合（Microsoft Office VBA コーディング規約）\n\n**スタイル**\n- [ ] インデントは4スペース（またはタブ）で統一されているか\n- [ ] 長い行は `_` で適切に折り返されているか\n\n**命名規則**\n- [ ] 変数名はハンガリアン記法またはキャメルケースで統一されているか\n- [ ] 定数名は `UPPER_SNAKE_CASE` か\n- [ ] プロシージャ名・関数名は動詞で始まる `PascalCase` か\n\n**コード品質**\n- [ ] `Option Explicit` が全モジュールに宣言されているか\n- [ ] 変数の型が明示的に宣言されているか（`Dim x As Long` 等）\n- [ ] エラーハンドリング（`On Error GoTo`）が適切か（`On Error Resume Next` の多用は不可）\n- [ ] マジックナンバーが使われていないか（`Const` で定数化されているか）\n- [ ] ワークシートへの参照が `ActiveSheet` に依存していないか\n\n**エンコーディング**\n- [ ] VBA ファイル（`.bas` / `.cls` / `.frm`）内の日本語はSHIFT-JIS（CP932）で出力されているか（UTF-8で書き込むと Windows VBAエディタで文字化けする）\n\n> ⚠️ **VBAファイルを修正・新規作成する場合は必ずSHIFT-JIS（CP932）で書き込むこと。**\n> Edit/Write ツールは UTF-8 で書き込むため、VBAファイルには使用禁止。\n> 代わりに以下の Bash コマンドで書き込む：\n> ```bash\n> python3 -c \"\n> content = '''（VBAコード）'''\n> with open('ファイルパス', 'w', encoding='cp932') as f:\n>     f.write(content)\n> \"\n> ```\n\n---\n\n#### Lua の場合\n\n**命名規則**\n- [ ] 変数・関数名は `snake_case` か\n- [ ] 定数は `UPPER_SNAKE_CASE` か\n\n**コード品質**\n- [ ] グローバル変数の使用が最小限か（`local` が適切に使われているか）\n- [ ] `pcall` / `xpcall` によるエラーハンドリングが適切か\n- [ ] `nil` チェックが適切に行われているか\n\n---\n\n#### Ruby の場合\n\n**命名規則**\n- [ ] 変数・メソッド名は `snake_case` か\n- [ ] クラス・モジュール名は `PascalCase` か\n\n**コード品質**\n- [ ] `rescue` で例外クラスが明示されているか（裸の `rescue` は不可）\n- [ ] テストが追加・更新されているか\n\n---\n\n### Step 5: Find Bug（バグ検出）\n\n以下の観点でコードを精査し、潜在的なバグを特定する：\n\n**境界値・数値**\n- [ ] ゼロ除算のリスクがないか（除算前に分母のゼロチェックがあるか）\n- [ ] 配列・コレクションの境界値アクセスが安全か（off-by-one エラー）\n- [ ] 数値オーバーフロー・アンダーフローのリスクがないか\n- [ ] 浮動小数点の比較に `==` を使っていないか\n\n**null / 未初期化**\n- [ ] null / Nothing / None 参照のリスクがないか\n- [ ] 変数が初期化される前に参照されていないか\n- [ ] 関数の戻り値が null / Nothing になりうる場合に呼び出し元でチェックしているか\n\n**条件分岐・制御フロー**\n- [ ] 条件分岐に漏れがないか（`else` / `default` の未処理ケース）\n- [ ] 複合条件の論理演算子（`And` / `Or` / `&&` / `||`）が意図通りか\n- [ ] `switch` / `Select Case` に `default` / `Case Else` があるか\n\n**ループ**\n- [ ] 無限ループになるリスクがないか（終了条件が必ず満たされるか）\n- [ ] ループカウンタの更新が適切か\n- [ ] コレクションをループ中に変更していないか\n\n**リソース管理**\n- [ ] ファイル・DB接続・オブジェクトが確実にクローズ・解放されているか\n- [ ] エラー発生時にもリソースが解放されるか（`Finally` / `On Error` での後処理）\n\n**非同期・並行処理**\n- [ ] 競合状態（Race Condition）のリスクがないか\n- [ ] 非同期処理の結果を待たずに次の処理に進んでいないか\n\n**その他**\n- [ ] コピー＆ペーストミス（似たブロックで変数名だけ変え忘れ等）がないか\n- [ ] 再帰処理に終了条件があるか（スタックオーバーフローのリスク）\n\n---\n\n### Step 6: スペルチェック\n\nコード内の識別子・コメント・文字列リテラルのスペルミスを検出する：\n\n**識別子（変数名・関数名・定数名・クラス名）**\n- [ ] 明らかなタイポがないか（例: `recieve` → `receive`、`caluculate` → `calculate`）\n- [ ] 省略形が一貫しているか（例: `msg` と `message` が混在していないか）\n- [ ] 単数・複数形が意味に沿っているか（例: コレクションは複数形）\n\n**コメント・文字列リテラル**\n- [ ] コメント内の英単語にスペルミスがないか\n- [ ] 日本語コメントに誤字・脱字がないか\n- [ ] ユーザー向けメッセージ・ログ文字列に誤字がないか\n\n---\n\n### Step 7: チーム固有ルールチェック\n\n#### ルール1: マジックバリューの排除（重要度で区別）\n\n**【重要度：高】ロジックに影響するもの → 厳しく指摘**\n- [ ] セルの列番号・行番号・セル位置が直書きされていないか\n- [ ] シート名（例: `\"Sheet1\"`, `\"Data\"`）が直書きされていないか\n- [ ] ファイルパス・ループ制御値が直書きされていないか\n\n**【重要度：低】表示のみのもの → 指摘優先度「低」**\n- [ ] メッセージ文言（例: `\"処理完了\"`, `\"エラーです\"`）の直書き（指摘はするが必須修正ではない）\n\n#### ルール2: DRY原則（重複コードの排除）\n- [ ] 似たような処理が2回以上登場している場合、関数へ切り出されているか\n- [ ] コピー＆ペーストで量産されたコードブロックがないか\n\n#### ルール3: モジュールの機能別分割（単一責任の原則）\n- [ ] 1つのモジュール・プロシージャに「UI/View操作」「ビジネスロジック」「インフラ/アクセス」が混在していないか\n- [ ] 異なる種類の責務が混在している場合、分離・委譲の構成を提案すること\n\n---\n\n### Step 8: ロジック・設計レビュー\n\n- バグやロジックの問題がないか\n- 可読性・保守性の観点で改善できる点はないか\n- パフォーマンス上の懸念点はないか（N+1クエリ、不要なループ等）\n- セキュリティ上のリスクはないか（入力バリデーション、SQLインジェクション等）\n- 既存コードとの一貫性が保たれているか\n\n---\n\n## 出力フォーマット\n\n```\n## コードレビュー結果\n\n**対象**: （レビューしたファイル名一覧）\n**対象言語**: 検出した言語\n\n---\n\n### 🚨 クリティカルな指摘（環境設定違反）\n| 行番号 | 重要度 | 指摘内容 |\n| :--- | :--- | :--- |\n| ... | **最高** | ... |\n（該当なければ「なし」）\n\n---\n\n### 共通チェック\n| 項目 | 結果 | コメント |\n|------|------|----------|\n| ... | OK / NG / 該当なし | ... |\n\n### 言語固有チェック（言語名）\n| 項目 | 結果 | コメント |\n|------|------|----------|\n| ... | OK / NG / 該当なし | ... |\n\n### Find Bug（バグ検出）\n（検出したバグ・潜在的リスクをファイル名・行番号付きで記載。問題なければ「特記事項なし」）\n\n### スペルチェック\n（スペルミス・誤字脱字をファイル名・行番号・修正案付きで記載。問題なければ「特記事項なし」）\n\n### ❌ チーム規約違反\n| 行番号 | 重要度 | 違反ルール | 指摘内容 |\n| :--- | :--- | :--- | :--- |\n| ... | **高** / **低** | ... | ... |\n（該当なければ「なし」）\n\n### ✅ 修正案（コードスニペット）\n（上記指摘を反映した修正コード。指摘がなければ省略）\n> ⚠️ VBAファイルを実際に修正する場合は Edit/Write ツールを使わず、必ず `python3 -c \"... encoding='cp932' ...\"` で SHIFT-JIS として書き込むこと。\n\n### ロジック・設計レビュー\n（気になった点を具体的に記載。問題なければ「特記事項なし」）\n\n### 改善提案（任意）\n- （必須ではないが改善すると良い点）\n\n---\n\n### 📝 総評\n- **総合評価**: 問題なし / 軽微な修正推奨 / 要修正\n- **品質スコア**: XX / 100\n- **コメント**: （全体を通じた所感・優先して対応すべき点）\n```",
    "path": "./code-copy/SKILL.md"
  },
  {
    "name": "code-test",
    "description": "これはテストの内容である。",
    "disable-model-invocation": "true",
    "context": "fork",
    "content": "# Code Review\n\n## レビュー対象の決定\n\n$ARGUMENTSが指定されている場合はそのファイル・ディレクトリを対象にする。\n- ファイルパスが渡された場合 → Read ツールでそのファイルを読む\n- ディレクトリパスが渡された場合 → Glob でファイル一覧を取得してから Read で各ファイルを読む\n\n$ARGUMENTSが指定されていない場合は、現在のワーキングディレクトリのコードファイルを対象にする：\n1. !`find . -type f \\( -name \"*.py\" -o -name \"*.js\" -o -name \"*.ts\" -o -name \"*.jsx\" -o -name \"*.tsx\" -o -name \"*.vue\" -o -name \"*.rb\" -o -name \"*.lua\" -o -name \"*.bas\" -o -name \"*.cls\" -o -name \"*.frm\" \\) -not -path \"*/node_modules/*\" -not -path \"*/.git/*\" -not -path \"*/__pycache__/*\" -not -path \"*/.venv/*\" 2>/dev/null | head -30`\n2. 上記で見つかったファイルを Read ツールで読む\n3. ファイルが多い場合は、ユーザーに「どのファイル・ディレクトリを対象にしますか？」と確認してから進める（目安: 5ファイル超は確認する）\n\n---\n\n## レビュー手順\n\n### Step 1: 言語・技術スタックの判定\n\n対象ファイルの拡張子から使用言語を判定する：\n- `.py` → Python\n- `.js` / `.ts` / `.jsx` / `.tsx` → JavaScript/TypeScript\n- `.vue` → Vue.js\n- `.bas` / `.cls` / `.frm` / `.vba` → VBA\n- `.lua` → Lua\n- `.rb` → Ruby\n- `.html` / `.hbs` → HTML/Handlebars\n- その他はファイル拡張子から判断\n\n---\n\n### Step 2: 🚨 環境設定チェック【最優先・最初に確認】\n\n**Monitoring モジュール等のURL設定を本番リリース前に必ず確認すること。**\n違反があれば他のチェックより先に最優先で警告を出す。\n\n> ⚠️ 本番URLがコメントアウトされている、または検証URLが有効になっている場合は **🚨 クリティカル** として報告する。\n\n---\n\n### Step 3: 共通チェック（全言語）\n\n- [ ] デバッグコードが残っていないか（`print`, `console.log`, `Debug.Print`, `pp` 等）\n- [ ] コメントアウトされたままの不要なコードが残っていないか\n- [ ] ハードコードされた認証情報・APIキー・パスワードがないか\n- [ ] 不要なファイル（`.DS_Store`, `__pycache__`, `*.pyc`, `node_modules` 等）が含まれていないか\n- [ ] 変数名・関数名が意味を適切に表しているか（`i`, `r`, `x`, `tmp`, `hoge` 等の1文字・無意味な名前は禁止）\n\n---\n\n### Step 4: 言語固有チェック\n\n#### Python の場合（PEP8 / Google Python Style Guide）\n\n**スタイル**\n- [ ] インデントは4スペースか\n- [ ] 1行の長さが適切か（PEP8: 79文字、black: 88文字が目安）\n- [ ] インポートは stdlib → サードパーティ → ローカルの順に整理されているか（isort）\n- [ ] `import *` を使っていないか\n- [ ] 不要なインポートがないか\n\n**命名規則**\n- [ ] 変数・関数名は `snake_case` か\n- [ ] クラス名は `PascalCase` か\n- [ ] 定数は `UPPER_SNAKE_CASE` か\n\n**コード品質**\n- [ ] 例外処理が適切か（裸の `except:` や `except Exception:` になっていないか）\n- [ ] `f-string` や適切な文字列フォーマットを使っているか\n- [ ] mutable なデフォルト引数（`def f(x=[]):`）を使っていないか\n- [ ] `is None` / `is not None` を使っているか（`== None` は不可）\n\n---\n\n#### JavaScript / TypeScript の場合（ESLint / Prettier）\n\n**スタイル**\n- [ ] インデントは2スペースか\n- [ ] 文末セミコロンのスタイルが統一されているか\n\n**命名規則**\n- [ ] 変数・関数名は `camelCase` か\n- [ ] クラス名は `PascalCase` か\n- [ ] 定数は `UPPER_SNAKE_CASE` か\n\n**コード品質**\n- [ ] `var` を使っていないか（`const` / `let` を使うべき）\n- [ ] 非同期処理が適切か（`async/await` を優先）\n- [ ] `==` ではなく `===` を使っているか\n- [ ] エラーハンドリングが適切か（Promise の `.catch()` や `try/catch`）\n\n**TypeScript 固有**\n- [ ] `any` 型を使っていないか（`unknown` を検討）\n- [ ] non-null アサーション（`!`）が過剰でないか\n\n**セキュリティ**\n- [ ] XSS リスクがないか（`innerHTML`, `dangerouslySetInnerHTML` の不適切な使用）\n- [ ] `eval()` を使っていないか\n\n---\n\n#### VBA の場合（Microsoft Office VBA コーディング規約）\n\n**スタイル**\n- [ ] インデントは4スペース（またはタブ）で統一されているか\n- [ ] 長い行は `_` で適切に折り返されているか\n\n**命名規則**\n- [ ] 変数名はハンガリアン記法またはキャメルケースで統一されているか\n- [ ] 定数名は `UPPER_SNAKE_CASE` か\n- [ ] プロシージャ名・関数名は動詞で始まる `PascalCase` か\n\n**コード品質**\n- [ ] `Option Explicit` が全モジュールに宣言されているか\n- [ ] 変数の型が明示的に宣言されているか（`Dim x As Long` 等）\n- [ ] エラーハンドリング（`On Error GoTo`）が適切か（`On Error Resume Next` の多用は不可）\n- [ ] マジックナンバーが使われていないか（`Const` で定数化されているか）\n- [ ] ワークシートへの参照が `ActiveSheet` に依存していないか\n\n**エンコーディング**\n- [ ] VBA ファイル（`.bas` / `.cls` / `.frm`）内の日本語はSHIFT-JIS（CP932）で出力されているか（UTF-8で書き込むと Windows VBAエディタで文字化けする）\n\n> ⚠️ **VBAファイルを修正・新規作成する場合は必ずSHIFT-JIS（CP932）で書き込むこと。**\n> Edit/Write ツールは UTF-8 で書き込むため、VBAファイルには使用禁止。\n> 代わりに以下の Bash コマンドで書き込む：\n> ```bash\n> python3 -c \"\n> content = '''（VBAコード）'''\n> with open('ファイルパス', 'w', encoding='cp932') as f:\n>     f.write(content)\n> \"\n> ```\n\n---\n\n#### Lua の場合\n\n**命名規則**\n- [ ] 変数・関数名は `snake_case` か\n- [ ] 定数は `UPPER_SNAKE_CASE` か\n\n**コード品質**\n- [ ] グローバル変数の使用が最小限か（`local` が適切に使われているか）\n- [ ] `pcall` / `xpcall` によるエラーハンドリングが適切か\n- [ ] `nil` チェックが適切に行われているか\n\n---\n\n#### Ruby の場合\n\n**命名規則**\n- [ ] 変数・メソッド名は `snake_case` か\n- [ ] クラス・モジュール名は `PascalCase` か\n\n**コード品質**\n- [ ] `rescue` で例外クラスが明示されているか（裸の `rescue` は不可）\n- [ ] テストが追加・更新されているか\n\n---\n\n### Step 5: Find Bug（バグ検出）\n\n以下の観点でコードを精査し、潜在的なバグを特定する：\n\n**境界値・数値**\n- [ ] ゼロ除算のリスクがないか（除算前に分母のゼロチェックがあるか）\n- [ ] 配列・コレクションの境界値アクセスが安全か（off-by-one エラー）\n- [ ] 数値オーバーフロー・アンダーフローのリスクがないか\n- [ ] 浮動小数点の比較に `==` を使っていないか\n\n**null / 未初期化**\n- [ ] null / Nothing / None 参照のリスクがないか\n- [ ] 変数が初期化される前に参照されていないか\n- [ ] 関数の戻り値が null / Nothing になりうる場合に呼び出し元でチェックしているか\n\n**条件分岐・制御フロー**\n- [ ] 条件分岐に漏れがないか（`else` / `default` の未処理ケース）\n- [ ] 複合条件の論理演算子（`And` / `Or` / `&&` / `||`）が意図通りか\n- [ ] `switch` / `Select Case` に `default` / `Case Else` があるか\n\n**ループ**\n- [ ] 無限ループになるリスクがないか（終了条件が必ず満たされるか）\n- [ ] ループカウンタの更新が適切か\n- [ ] コレクションをループ中に変更していないか\n\n**リソース管理**\n- [ ] ファイル・DB接続・オブジェクトが確実にクローズ・解放されているか\n- [ ] エラー発生時にもリソースが解放されるか（`Finally` / `On Error` での後処理）\n\n**非同期・並行処理**\n- [ ] 競合状態（Race Condition）のリスクがないか\n- [ ] 非同期処理の結果を待たずに次の処理に進んでいないか\n\n**その他**\n- [ ] コピー＆ペーストミス（似たブロックで変数名だけ変え忘れ等）がないか\n- [ ] 再帰処理に終了条件があるか（スタックオーバーフローのリスク）\n\n---\n\n### Step 6: スペルチェック\n\nコード内の識別子・コメント・文字列リテラルのスペルミスを検出する：\n\n**識別子（変数名・関数名・定数名・クラス名）**\n- [ ] 明らかなタイポがないか（例: `recieve` → `receive`、`caluculate` → `calculate`）\n- [ ] 省略形が一貫しているか（例: `msg` と `message` が混在していないか）\n- [ ] 単数・複数形が意味に沿っているか（例: コレクションは複数形）\n\n**コメント・文字列リテラル**\n- [ ] コメント内の英単語にスペルミスがないか\n- [ ] 日本語コメントに誤字・脱字がないか\n- [ ] ユーザー向けメッセージ・ログ文字列に誤字がないか\n\n---\n\n### Step 7: チーム固有ルールチェック\n\n#### ルール1: マジックバリューの排除（重要度で区別）\n\n**【重要度：高】ロジックに影響するもの → 厳しく指摘**\n- [ ] セルの列番号・行番号・セル位置が直書きされていないか\n- [ ] シート名（例: `\"Sheet1\"`, `\"Data\"`）が直書きされていないか\n- [ ] ファイルパス・ループ制御値が直書きされていないか\n\n**【重要度：低】表示のみのもの → 指摘優先度「低」**\n- [ ] メッセージ文言（例: `\"処理完了\"`, `\"エラーです\"`）の直書き（指摘はするが必須修正ではない）\n\n#### ルール2: DRY原則（重複コードの排除）\n- [ ] 似たような処理が2回以上登場している場合、関数へ切り出されているか\n- [ ] コピー＆ペーストで量産されたコードブロックがないか\n\n#### ルール3: モジュールの機能別分割（単一責任の原則）\n- [ ] 1つのモジュール・プロシージャに「UI/View操作」「ビジネスロジック」「インフラ/アクセス」が混在していないか\n- [ ] 異なる種類の責務が混在している場合、分離・委譲の構成を提案すること\n\n---\n\n### Step 8: ロジック・設計レビュー\n\n- バグやロジックの問題がないか\n- 可読性・保守性の観点で改善できる点はないか\n- パフォーマンス上の懸念点はないか（N+1クエリ、不要なループ等）\n- セキュリティ上のリスクはないか（入力バリデーション、SQLインジェクション等）\n- 既存コードとの一貫性が保たれているか\n\n---\n\n## 出力フォーマット\n\n```\n## コードレビュー結果\n\n**対象**: （レビューしたファイル名一覧）\n**対象言語**: 検出した言語\n\n---\n\n### 🚨 クリティカルな指摘（環境設定違反）\n| 行番号 | 重要度 | 指摘内容 |\n| :--- | :--- | :--- |\n| ... | **最高** | ... |\n（該当なければ「なし」）\n\n---\n\n### 共通チェック\n| 項目 | 結果 | コメント |\n|------|------|----------|\n| ... | OK / NG / 該当なし | ... |\n\n### 言語固有チェック（言語名）\n| 項目 | 結果 | コメント |\n|------|------|----------|\n| ... | OK / NG / 該当なし | ... |\n\n### Find Bug（バグ検出）\n（検出したバグ・潜在的リスクをファイル名・行番号付きで記載。問題なければ「特記事項なし」）\n\n### スペルチェック\n（スペルミス・誤字脱字をファイル名・行番号・修正案付きで記載。問題なければ「特記事項なし」）\n\n### ❌ チーム規約違反\n| 行番号 | 重要度 | 違反ルール | 指摘内容 |\n| :--- | :--- | :--- | :--- |\n| ... | **高** / **低** | ... | ... |\n（該当なければ「なし」）\n\n### ✅ 修正案（コードスニペット）\n（上記指摘を反映した修正コード。指摘がなければ省略）\n> ⚠️ VBAファイルを実際に修正する場合は Edit/Write ツールを使わず、必ず `python3 -c \"... encoding='cp932' ...\"` で SHIFT-JIS として書き込むこと。\n\n### ロジック・設計レビュー\n（気になった点を具体的に記載。問題なければ「特記事項なし」）\n\n### 改善提案（任意）\n- （必須ではないが改善すると良い点）\n\n---\n\n### 📝 総評\n- **総合評価**: 問題なし / 軽微な修正推奨 / 要修正\n- **品質スコア**: XX / 100\n- **コメント**: （全体を通じた所感・優先して対応すべき点）\n```",
    "path": "./code-test/SKILL.md"
  },
  {
    "name": "auto-news",
    "description": "これはテストの内容である。",
    "disable-model-invocation": "true",
    "context": "fork",
    "content": "# Code Review\n\n## レビュー対象の決定\n\n$ARGUMENTSが指定されている場合はそのファイル・ディレクトリを対象にする。\n- ファイルパスが渡された場合 → Read ツールでそのファイルを読む\n- ディレクトリパスが渡された場合 → Glob でファイル一覧を取得してから Read で各ファイルを読む\n\n$ARGUMENTSが指定されていない場合は、現在のワーキングディレクトリのコードファイルを対象にする：\n1. !`find . -type f \\( -name \"*.py\" -o -name \"*.js\" -o -name \"*.ts\" -o -name \"*.jsx\" -o -name \"*.tsx\" -o -name \"*.vue\" -o -name \"*.rb\" -o -name \"*.lua\" -o -name \"*.bas\" -o -name \"*.cls\" -o -name \"*.frm\" \\) -not -path \"*/node_modules/*\" -not -path \"*/.git/*\" -not -path \"*/__pycache__/*\" -not -path \"*/.venv/*\" 2>/dev/null | head -30`\n2. 上記で見つかったファイルを Read ツールで読む\n3. ファイルが多い場合は、ユーザーに「どのファイル・ディレクトリを対象にしますか？」と確認してから進める（目安: 5ファイル超は確認する）\n\n---\n\n## レビュー手順\n\n### Step 1: 言語・技術スタックの判定\n\n対象ファイルの拡張子から使用言語を判定する：\n- `.py` → Python\n- `.js` / `.ts` / `.jsx` / `.tsx` → JavaScript/TypeScript\n- `.vue` → Vue.js\n- `.bas` / `.cls` / `.frm` / `.vba` → VBA\n- `.lua` → Lua\n- `.rb` → Ruby\n- `.html` / `.hbs` → HTML/Handlebars\n- その他はファイル拡張子から判断\n\n---\n\n### Step 2: 🚨 環境設定チェック【最優先・最初に確認】\n\n**Monitoring モジュール等のURL設定を本番リリース前に必ず確認すること。**\n違反があれば他のチェックより先に最優先で警告を出す。\n\n> ⚠️ 本番URLがコメントアウトされている、または検証URLが有効になっている場合は **🚨 クリティカル** として報告する。\n\n---\n\n### Step 3: 共通チェック（全言語）\n\n- [ ] デバッグコードが残っていないか（`print`, `console.log`, `Debug.Print`, `pp` 等）\n- [ ] コメントアウトされたままの不要なコードが残っていないか\n- [ ] ハードコードされた認証情報・APIキー・パスワードがないか\n- [ ] 不要なファイル（`.DS_Store`, `__pycache__`, `*.pyc`, `node_modules` 等）が含まれていないか\n- [ ] 変数名・関数名が意味を適切に表しているか（`i`, `r`, `x`, `tmp`, `hoge` 等の1文字・無意味な名前は禁止）\n\n---\n\n### Step 4: 言語固有チェック\n\n#### Python の場合（PEP8 / Google Python Style Guide）\n\n**スタイル**\n- [ ] インデントは4スペースか\n- [ ] 1行の長さが適切か（PEP8: 79文字、black: 88文字が目安）\n- [ ] インポートは stdlib → サードパーティ → ローカルの順に整理されているか（isort）\n- [ ] `import *` を使っていないか\n- [ ] 不要なインポートがないか\n\n**命名規則**\n- [ ] 変数・関数名は `snake_case` か\n- [ ] クラス名は `PascalCase` か\n- [ ] 定数は `UPPER_SNAKE_CASE` か\n\n**コード品質**\n- [ ] 例外処理が適切か（裸の `except:` や `except Exception:` になっていないか）\n- [ ] `f-string` や適切な文字列フォーマットを使っているか\n- [ ] mutable なデフォルト引数（`def f(x=[]):`）を使っていないか\n- [ ] `is None` / `is not None` を使っているか（`== None` は不可）\n\n---\n\n#### JavaScript / TypeScript の場合（ESLint / Prettier）\n\n**スタイル**\n- [ ] インデントは2スペースか\n- [ ] 文末セミコロンのスタイルが統一されているか\n\n**命名規則**\n- [ ] 変数・関数名は `camelCase` か\n- [ ] クラス名は `PascalCase` か\n- [ ] 定数は `UPPER_SNAKE_CASE` か\n\n**コード品質**\n- [ ] `var` を使っていないか（`const` / `let` を使うべき）\n- [ ] 非同期処理が適切か（`async/await` を優先）\n- [ ] `==` ではなく `===` を使っているか\n- [ ] エラーハンドリングが適切か（Promise の `.catch()` や `try/catch`）\n\n**TypeScript 固有**\n- [ ] `any` 型を使っていないか（`unknown` を検討）\n- [ ] non-null アサーション（`!`）が過剰でないか\n\n**セキュリティ**\n- [ ] XSS リスクがないか（`innerHTML`, `dangerouslySetInnerHTML` の不適切な使用）\n- [ ] `eval()` を使っていないか\n\n---\n\n#### VBA の場合（Microsoft Office VBA コーディング規約）\n\n**スタイル**\n- [ ] インデントは4スペース（またはタブ）で統一されているか\n- [ ] 長い行は `_` で適切に折り返されているか\n\n**命名規則**\n- [ ] 変数名はハンガリアン記法またはキャメルケースで統一されているか\n- [ ] 定数名は `UPPER_SNAKE_CASE` か\n- [ ] プロシージャ名・関数名は動詞で始まる `PascalCase` か\n\n**コード品質**\n- [ ] `Option Explicit` が全モジュールに宣言されているか\n- [ ] 変数の型が明示的に宣言されているか（`Dim x As Long` 等）\n- [ ] エラーハンドリング（`On Error GoTo`）が適切か（`On Error Resume Next` の多用は不可）\n- [ ] マジックナンバーが使われていないか（`Const` で定数化されているか）\n- [ ] ワークシートへの参照が `ActiveSheet` に依存していないか\n\n**エンコーディング**\n- [ ] VBA ファイル（`.bas` / `.cls` / `.frm`）内の日本語はSHIFT-JIS（CP932）で出力されているか（UTF-8で書き込むと Windows VBAエディタで文字化けする）\n\n> ⚠️ **VBAファイルを修正・新規作成する場合は必ずSHIFT-JIS（CP932）で書き込むこと。**\n> Edit/Write ツールは UTF-8 で書き込むため、VBAファイルには使用禁止。\n> 代わりに以下の Bash コマンドで書き込む：\n> ```bash\n> python3 -c \"\n> content = '''（VBAコード）'''\n> with open('ファイルパス', 'w', encoding='cp932') as f:\n>     f.write(content)\n> \"\n> ```\n\n---\n\n#### Lua の場合\n\n**命名規則**\n- [ ] 変数・関数名は `snake_case` か\n- [ ] 定数は `UPPER_SNAKE_CASE` か\n\n**コード品質**\n- [ ] グローバル変数の使用が最小限か（`local` が適切に使われているか）\n- [ ] `pcall` / `xpcall` によるエラーハンドリングが適切か\n- [ ] `nil` チェックが適切に行われているか\n\n---\n\n#### Ruby の場合\n\n**命名規則**\n- [ ] 変数・メソッド名は `snake_case` か\n- [ ] クラス・モジュール名は `PascalCase` か\n\n**コード品質**\n- [ ] `rescue` で例外クラスが明示されているか（裸の `rescue` は不可）\n- [ ] テストが追加・更新されているか\n\n---\n\n### Step 5: Find Bug（バグ検出）\n\n以下の観点でコードを精査し、潜在的なバグを特定する：\n\n**境界値・数値**\n- [ ] ゼロ除算のリスクがないか（除算前に分母のゼロチェックがあるか）\n- [ ] 配列・コレクションの境界値アクセスが安全か（off-by-one エラー）\n- [ ] 数値オーバーフロー・アンダーフローのリスクがないか\n- [ ] 浮動小数点の比較に `==` を使っていないか\n\n**null / 未初期化**\n- [ ] null / Nothing / None 参照のリスクがないか\n- [ ] 変数が初期化される前に参照されていないか\n- [ ] 関数の戻り値が null / Nothing になりうる場合に呼び出し元でチェックしているか\n\n**条件分岐・制御フロー**\n- [ ] 条件分岐に漏れがないか（`else` / `default` の未処理ケース）\n- [ ] 複合条件の論理演算子（`And` / `Or` / `&&` / `||`）が意図通りか\n- [ ] `switch` / `Select Case` に `default` / `Case Else` があるか\n\n**ループ**\n- [ ] 無限ループになるリスクがないか（終了条件が必ず満たされるか）\n- [ ] ループカウンタの更新が適切か\n- [ ] コレクションをループ中に変更していないか\n\n**リソース管理**\n- [ ] ファイル・DB接続・オブジェクトが確実にクローズ・解放されているか\n- [ ] エラー発生時にもリソースが解放されるか（`Finally` / `On Error` での後処理）\n\n**非同期・並行処理**\n- [ ] 競合状態（Race Condition）のリスクがないか\n- [ ] 非同期処理の結果を待たずに次の処理に進んでいないか\n\n**その他**\n- [ ] コピー＆ペーストミス（似たブロックで変数名だけ変え忘れ等）がないか\n- [ ] 再帰処理に終了条件があるか（スタックオーバーフローのリスク）\n\n---\n\n### Step 6: スペルチェック\n\nコード内の識別子・コメント・文字列リテラルのスペルミスを検出する：\n\n**識別子（変数名・関数名・定数名・クラス名）**\n- [ ] 明らかなタイポがないか（例: `recieve` → `receive`、`caluculate` → `calculate`）\n- [ ] 省略形が一貫しているか（例: `msg` と `message` が混在していないか）\n- [ ] 単数・複数形が意味に沿っているか（例: コレクションは複数形）\n\n**コメント・文字列リテラル**\n- [ ] コメント内の英単語にスペルミスがないか\n- [ ] 日本語コメントに誤字・脱字がないか\n- [ ] ユーザー向けメッセージ・ログ文字列に誤字がないか\n\n---\n\n### Step 7: チーム固有ルールチェック\n\n#### ルール1: マジックバリューの排除（重要度で区別）\n\n**【重要度：高】ロジックに影響するもの → 厳しく指摘**\n- [ ] セルの列番号・行番号・セル位置が直書きされていないか\n- [ ] シート名（例: `\"Sheet1\"`, `\"Data\"`）が直書きされていないか\n- [ ] ファイルパス・ループ制御値が直書きされていないか\n\n**【重要度：低】表示のみのもの → 指摘優先度「低」**\n- [ ] メッセージ文言（例: `\"処理完了\"`, `\"エラーです\"`）の直書き（指摘はするが必須修正ではない）\n\n#### ルール2: DRY原則（重複コードの排除）\n- [ ] 似たような処理が2回以上登場している場合、関数へ切り出されているか\n- [ ] コピー＆ペーストで量産されたコードブロックがないか\n\n#### ルール3: モジュールの機能別分割（単一責任の原則）\n- [ ] 1つのモジュール・プロシージャに「UI/View操作」「ビジネスロジック」「インフラ/アクセス」が混在していないか\n- [ ] 異なる種類の責務が混在している場合、分離・委譲の構成を提案すること\n\n---\n\n### Step 8: ロジック・設計レビュー\n\n- バグやロジックの問題がないか\n- 可読性・保守性の観点で改善できる点はないか\n- パフォーマンス上の懸念点はないか（N+1クエリ、不要なループ等）\n- セキュリティ上のリスクはないか（入力バリデーション、SQLインジェクション等）\n- 既存コードとの一貫性が保たれているか\n\n---\n\n## 出力フォーマット\n\n```\n## コードレビュー結果\n\n**対象**: （レビューしたファイル名一覧）\n**対象言語**: 検出した言語\n\n---\n\n### 🚨 クリティカルな指摘（環境設定違反）\n| 行番号 | 重要度 | 指摘内容 |\n| :--- | :--- | :--- |\n| ... | **最高** | ... |\n（該当なければ「なし」）\n\n---\n\n### 共通チェック\n| 項目 | 結果 | コメント |\n|------|------|----------|\n| ... | OK / NG / 該当なし | ... |\n\n### 言語固有チェック（言語名）\n| 項目 | 結果 | コメント |\n|------|------|----------|\n| ... | OK / NG / 該当なし | ... |\n\n### Find Bug（バグ検出）\n（検出したバグ・潜在的リスクをファイル名・行番号付きで記載。問題なければ「特記事項なし」）\n\n### スペルチェック\n（スペルミス・誤字脱字をファイル名・行番号・修正案付きで記載。問題なければ「特記事項なし」）\n\n### ❌ チーム規約違反\n| 行番号 | 重要度 | 違反ルール | 指摘内容 |\n| :--- | :--- | :--- | :--- |\n| ... | **高** / **低** | ... | ... |\n（該当なければ「なし」）\n\n### ✅ 修正案（コードスニペット）\n（上記指摘を反映した修正コード。指摘がなければ省略）\n> ⚠️ VBAファイルを実際に修正する場合は Edit/Write ツールを使わず、必ず `python3 -c \"... encoding='cp932' ...\"` で SHIFT-JIS として書き込むこと。\n\n### ロジック・設計レビュー\n（気になった点を具体的に記載。問題なければ「特記事項なし」）\n\n### 改善提案（任意）\n- （必須ではないが改善すると良い点）\n\n---\n\n### 📝 総評\n- **総合評価**: 問題なし / 軽微な修正推奨 / 要修正\n- **品質スコア**: XX / 100\n- **コメント**: （全体を通じた所感・優先して対応すべき点）\n```",
    "path": "./auto-news/SKILL.md"
  }
]